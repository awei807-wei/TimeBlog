'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { API } from '@/lib/api';
export default function LoginPage(){const router=useRouter();const [password,setPassword]=useState('');const [code,setCode]=useState('');const [challenge,setChallenge]=useState('');const [step,setStep]=useState(1);const [error,setError]=useState('');const [notice,setNotice]=useState('');useEffect(()=>{const timer=window.setTimeout(()=>{if(new URLSearchParams(window.location.search).get('recovered')==='1')setNotice('密码已更新，请使用新密码登录；登录后仍需 TOTP 验证。')},0);return()=>window.clearTimeout(timer)},[]);async function next(){setError('');const body=step===1?{password}:{code,challenge};const r=await fetch(`${API}/auth/login/${step===1?'password':'totp'}`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(r.status===429){setError('尝试次数过多，请稍后重试。');return}if(!r.ok){if(step===2&&r.status===401){setCode('');setChallenge('');setStep(1);setError('验证码无效或登录挑战已失效，请重新输入密码。');return}setError('凭据不正确');return}const data=await r.json();if(step===1){setChallenge(data.challenge||'');setStep(2)}else{router.push('/admin')}}return <main id="main-content" className="shell"><div className="login"><Link href="/" className="brand">菜鸟手记</Link><h1>作者登录</h1><p className="note">需要密码与一次性验证码。不会在浏览器本地保存认证令牌。</p>{notice&&<div className="status" role="status">{notice}</div>}{step===1?<div className="field"><label htmlFor="password">密码</label><input id="password" type="password" value={password} autoFocus onChange={e=>setPassword(e.target.value)}/></div>:<div className="field"><label htmlFor="code">TOTP 验证码</label><input id="code" inputMode="numeric" autoFocus value={code} onChange={e=>setCode(e.target.value)}/></div>}{error&&<div className="error" role="alert">{error}</div>}<button className="primary" onClick={next}>{step===1?'继续':'登录'}</button></div></main>}
